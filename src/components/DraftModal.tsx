// 강의 추가 / 수정 모달
import * as s from './DraftModalStyle';

// const DraftModal:React.FC<> = ({}) => {
const DraftModal:React.FC = ({}) => {

    return (
        <s.ModalContainer>

        </s.ModalContainer>
    )
}

export default DraftModal;